<% 
    const pageProps = {
        title: "MobiStore - Dashboard - Product Update",
        metaData: [
            {
                name: "description",
                content: "Ecommerce website for selling mobiles, tablets and laptops"
            }
        ]
    }
%> 
<%-include('../layout/head.ejs', {pageProps}) %>

<div class="app">
    <%-include('../components/nav.ejs', {activePage: "dashboard", navClass: 'fixed', showCart: false}) %>
    <div class="container pt-lg">
        
        <h2 class="text-center mb-3">Add Product</h2>
        <form class="my-5 product-form" id="add-product-form" enctype="multipart/form-data">
            <div class="row">
                <div class="col-12 col-lg-6 mb-4">
                    <label for="addProductName">Name</label>
                    <input type="text" name="addProductName" id="addProductName" class="form-control" required>
                </div>
                <div class="col-12 col-lg-6 mb-4">
                    <label for="addProductPrice">Price</label>
                    <input type="number" name="addProductPrice" id="addProductPrice" class="form-control"required>
                </div>
                <div class="col-12 mb-4">
                    <label for="addProductDescription">Description</label>
                    <textarea name="addProductDescription" id="addProductDescription" class="form-control" rows="5" required></textarea>
                </div>
                
                <div class="col-12 mb-4">
                    <div class="row">
                        <div class="col-12 col-lg-6">
                            <label for="addProductImage">Add Image</label>
                            <input  class="d-block" type="file" id="addProductImage" name="addProductImage" accept="image/*" required>
                        </div>
                    </div>
                </div>
            </div>
            <div class="text-center mt-5">
                <button type="submit" class="btn btn-primary px-5">Add Product</a>
            </div>
        </form>
    </div>
    <%-include('../sections/footer.ejs') %>
</div>

<%-include('../layout/footer.ejs') %>
